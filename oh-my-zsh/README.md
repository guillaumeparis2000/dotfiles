Add custom theme that show the current folder path, the git repository status and the last command execution status.

Load custom plugins: ```git battery cp svn-fast-info bower composer gem docker brew```
